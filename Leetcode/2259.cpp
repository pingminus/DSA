#include <string>
using namespace std;


class Solution {
public:
    string removeDigit(string number, char digit) {
        int maxNum = -1;
        int maxIndex = -1;
        for(int i = 0; i < number.size(); i++){
            if(number[i] == digit){
                if()
        }


    }
};